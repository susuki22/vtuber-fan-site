<div>
  <p><%= attachment_image_tag @tweet, :image, :fill,100,100 ,fallback: "no_image.jpg" %></p>
  <p>ユーザーネーム：<%= @tweet.user.name %></p>
  <p>ツイート：<%= @tweet.tweet_text%></p>
  <p>投稿日時：<%= @tweet.created_at.strftime('%Y/%m/%d') %></p>
</div>
