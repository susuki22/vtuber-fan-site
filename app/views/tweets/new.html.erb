<h1>画像投稿フォーム</h1>
<%= form_with model: @tweet, local:true do |f| %>
  <h4>ツイート文</h4>
  <%= f.text_area :tweet_text %>
  <h4>投稿画像</h4>
  <%= f.attachment_field :image %>
  <%= f.submit '投稿' %>
<% end %>
